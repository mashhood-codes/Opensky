# Opensky
A decentralized social media platform built with AT Protocol, Apollo Federation, and microservices architecture. Features distributed PDS nodes, Redis fan-out caching, IPFS storage, and GraphQL API gateway.

The video file is large so to see code only, you click on rar file and download that raw file only.
